import NavBar from "../../NavBar";

export default function Header() {
  return (
    <header className="sticky top-0 z-40 flex w-full items-center justify-between">
      <NavBar />
    </header>
  );
}
